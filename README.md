# App Cloner

An Android app that lists installed apps and saves their APK files to
`Downloads/ClonedApps/`.

## Build
APKs are built automatically by GitHub Actions on every push.

## Install
Download the latest `app-debug` artifact from the Actions tab,
unzip it, and install `app-debug.apk` on your Android phone.
